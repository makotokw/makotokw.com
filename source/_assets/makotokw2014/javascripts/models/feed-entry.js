/*global makotokw, Backbone*/

makotokw.Models = makotokw.Models || {};

(function () {
    'use strict';
    makotokw.Models.FeedEntryModel = Backbone.Model.extend({
        initialize: function() {
        }
    });
})();
